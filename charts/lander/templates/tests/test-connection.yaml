apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lander.fullname" . }}-test-connection"
  labels:
    {{- include "lander.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lander.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
