apiVersion: v1
kind: Service
metadata:
  name: {{ include "lander.fullname" . }}
  labels:
    {{- include "lander.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lander.selectorLabels" . | nindent 4 }}
